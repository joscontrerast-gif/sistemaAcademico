server:
  port: 8085

spring:
  application:
    name: ms-asistencia

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/asistencia_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8761/eureka/

  instance:
    prefer-ip-address: true