server:
  port: 8081

spring:
  application:
    name: ms-usuarios

  datasource:
    url: jdbc:mysql://localhost:3306/usuarios_db
    username: root
    password: ""

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true

jwt:
  secret: miClaveSuperSecretaQueDebeSerLargaYSegura123456
  expiration: 86400000